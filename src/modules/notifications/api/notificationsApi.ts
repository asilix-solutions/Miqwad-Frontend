/**
 * @file notificationsApi.ts
 *
 * // TODO: wire to backend — there is no persisted notifications REST API
 * // yet (GET /api/notifications, GET /api/notifications, /unread-count,
 * // /read-all all 404 verified). Once the backend ships these, add
 * // TanStack Query hooks here (list, unread-count) and mutations
 * // (mark-read, mark-all-read) following the pattern in
 * // ../../chat/api/chatApi.ts, and switch ../store/notificationsSlice.ts
 * // from the sole source of truth to a cache seeded/synced by REST.
 *
 * Left as an empty stub deliberately — no fabricated requests against
 * endpoints that don't exist.
 */

export {};
